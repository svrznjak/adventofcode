/**
 * This file contains tests for helper functions
 */

import * as Helpers from './helpers.js';

describe('helpers/test', () => {
  test('It tests nothing', async () => {
    expect(0).toEqual(0);
  });
});
